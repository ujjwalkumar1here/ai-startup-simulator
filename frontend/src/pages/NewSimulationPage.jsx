import { useState } from "react"; import { useForm } from "react-hook-form"; import { useNavigate } from "react-router-dom"; import { motion } from "framer-motion"; import toast from "react-hot-toast"; import { Rocket } from "lucide-react"; import Input from "../components/ui/Input.jsx"; import Textarea from "../components/ui/Textarea.jsx"; import Button from "../components/ui/Button.jsx"; import simulationService from "../services/simulationService.js";
const fields = [["startupName", "Startup Name", "e.g. FitAI", Input], ["industry", "Industry", "e.g. Health & Fitness Tech", Input], ["problem", "Problem", "What problem are you solving?", Textarea], ["solution", "Solution", "How does your product solve this problem?", Textarea], ["targetAudience", "Target Audience", "Who is your ideal customer?", Textarea], ["businessModel", "Business Model", "e.g. Freemium subscription", Input], ["revenueModel", "Revenue Model", "How exactly will you generate revenue?", Textarea], ["fundingRequired", "Funding Required", "e.g. $50,000 seed round", Input], ["competitors", "Competitors", "Who are your main competitors?", Textarea], ["usp", "Unique Selling Proposition", "What makes you different from competitors?", Textarea], ["technologyStack", "Technology Stack", "What technologies will power your product?", Textarea], ["vision", "Vision", "What is your long-term vision for this startup?", Textarea]];
export default function NewSimulationPage() { const { register, handleSubmit, formState: { errors } } = useForm(); const [loading, setLoading] = useState(false); const navigate = useNavigate(); const submit = async data => { setLoading(true); try { const idea = [`Solution: ${data.solution.trim()}`, `Revenue Model: ${data.revenueModel.trim()}`, `Competitors: ${data.competitors.trim()}`, `Technology Stack: ${data.technologyStack.trim()}`, `Vision: ${data.vision.trim()}`].join("\n\n"); const response = await simulationService.createSimulation({ startupName: data.startupName, idea, industry: data.industry, targetAudience: data.targetAudience, businessModel: data.businessModel, pricing: data.fundingRequired, problemStatement: data.problem, uniqueSellingProposition: data.usp }); toast.success("Simulation completed successfully!"); navigate(`/simulations/${response.data._id}`); } catch (error) { toast.error(!error?.response ? "Network error. Please check your connection." : error.response.data?.errors?.[0]?.message || error.response.data?.message || "Failed to run simulation. Please try again."); } finally { setLoading(false); } }; return <div className="mx-auto max-w-3xl"><div className="mb-6"><h1 className="text-2xl font-bold">New Simulation</h1><p className="mt-1 text-sm text-slate-500">Describe your startup idea in detail and let AI evaluate it like a venture capitalist.</p></div><motion.form initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} onSubmit={handleSubmit(submit)} className="space-y-5 rounded-2xl border border-slate-100 bg-white p-6 shadow-card sm:p-8">{fields.map(([name, label, placeholder, Component]) => <Component key={name} label={label} placeholder={placeholder} rows={3} error={errors[name]?.message} registration={register(name, { required: `${label} is required` })} />)}<Button type="submit" fullWidth loading={loading}><Rocket size={16} />Run AI Simulation</Button></motion.form></div>; }
